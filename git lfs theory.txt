steps fir git LFS
1. git lfs install # intialize git lfs where you want to upload on cloud.
2. git lfs track "*.psd" # add extension of the file ex. like .psd
3.git add .gitattributes # compulsory to run this command after git lfs tracking
4.git add file.psd  # here add the .extension file 
5. git commit -m "Add design file" # commit the changes
6.git push origin main  # puch the changes 
7.    
8.

